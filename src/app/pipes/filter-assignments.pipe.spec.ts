import { FilterAssignmentsPipe } from './filter-assignments.pipe';

describe('FilterAssignmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssignmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
